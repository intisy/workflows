# This repository's own README run, not the reusable workflow. See self-test.yml.
name: "Self: readme"

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  readme:
    permissions:
      contents: write
    uses: ./.github/workflows/readme.yml
    with:
      repository: ${{ github.repository }}
