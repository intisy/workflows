name: Generate README
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  readme:
    permissions:
      contents: write
    uses: ./.github/workflows/readme.yml
    with:
      repository: ${{ github.repository }}
