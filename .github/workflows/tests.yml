name: Tests

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/actions/**'
  pull_request:
    paths:
      - '.github/actions/**'

jobs:
  actions:
    uses: ./.github/workflows/test.yml
    with:
      toolchains: node
      node_build: ''
